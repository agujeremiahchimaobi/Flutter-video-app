import 'package:flutter/material.dart';

class Strings {
  static const introText =
      "I dont have anything to say right now \nor na your hand e dey to add the \ndesired text";
  static const welcomeText = "Welcome on Board!";
  static const welcomeStyle =
      TextStyle(fontSize: 30, fontWeight: FontWeight.bold);
  static const introStyle = TextStyle(fontSize: 18);
  static const title = 'VideoApp';
  static const titleStyle =
      TextStyle(fontSize: 30, fontWeight: FontWeight.bold, color: Colors.teal);
}
